(jbuild_version 1)

(executable
 ((name test_main)
  (public_name ppx-lens-runtest)
  (modes (byte))
  (preprocess
   (pps (ppx_lens)))
  (libraries
   (oUnit))))

(alias
 ((name runtest)
  (deps ())
  (action
   (run ocamlrun ${exe:test_main.bc}))))
